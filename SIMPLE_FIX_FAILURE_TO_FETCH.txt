══════════════════════════════════════════════════════════════
  "Failure to fetch" সমস্যার সমাধান - সহজ ভাষায়
══════════════════════════════════════════════════════════════

❌ সমস্যা কি?
─────────────
আপনি Expo Go এ App open করলে:
"Failure to fetch" দেখাচ্ছে

কারণ কি?
─────────
Frontend (Phone) এ:
  fetch('http://localhost:3000/...')
       ↓
Phone এর "localhost" মানে phone নিজেই
Phone এ কোনো server নেই
তাই connect হচ্ছে না ❌

সমাধান - 3 Easy Steps:
─────────────────────

STEP 1: Backend চালু করুন
─────────────────────────
Terminal খুলুন (নতুন):
  node server.js

দেখবেন:
  🌙 Smart Muslim Backend running on port 3000

এটা চালু রাখুন! বন্ধ করবেন না।


STEP 2: Frontend চালু করুন
──────────────────────────
অন্য Terminal খুলুন:
  npm run dev

দেখবেন:
  › Metro waiting on exp://...
  QR code
  › Web is waiting on http://localhost:8081


STEP 3: Expo Go এ test করুন
────────────────────────────
1. Expo Go app খুলুন
2. QR code scan করুন
3. App load হবে
4. Quran Reader Screen এ যান
5. সূরা ফাতিহা load হবে ✅


কেন এটা কাজ করে?
──────────────────
localhost:3000 = Backend server (একই machine)
fetch from Frontend = Frontend পায় ✓
Both on same machine = সংযোগ কাজ করে ✅


Terminal সেটআপ Example:
─────────────────────

┌─────────────────────┬─────────────────────┐
│  Terminal 1         │  Terminal 2         │
├─────────────────────┼─────────────────────┤
│ $ node server.js    │ $ npm run dev       │
│                     │                     │
│ ✅ Backend running  │ ✅ Frontend bundled │
│    port 3000        │    port 8081        │
│                     │                     │
│ (রাখুন চলমান)       │ (রাখুন চলমান)      │
└─────────────────────┴─────────────────────┘

এই দুটি Terminal সবসময় খোলা রাখুন!


ভবিষ্যতে কি হবে?
─────────────────
এখন: Local testing দুটি Terminal দিয়ে
পরে: Hosted server এ সরবে (একটা Terminal যথেষ্ট)


এখনই করুন:
───────────
1. ✓ server.js চালু করুন
2. ✓ npm run dev চালু করুন
3. ✓ QR scan করুন
4. ✓ সূরা দেখুন ✅

═══════════════════════════════════════════════════════════════
